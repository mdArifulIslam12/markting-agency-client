import React from 'react';
import Outstanding from '../../components/outstanding/Outstanding';

const Home = () => {
    return (
        <div>
            <Outstanding/>
        </div>
    );
};

export default Home;